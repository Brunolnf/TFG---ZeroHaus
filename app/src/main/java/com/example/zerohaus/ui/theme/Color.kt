package com.example.zerohaus.ui.theme

/**
 * La paleta de colores de ZeroHaus está definida íntegramente en Theme.kt
 * dentro de LightColorScheme / DarkColorScheme. Este archivo se mantiene
 * vacío por convención de proyecto Compose (el template lo exige), pero
 * no declara ningún color suelto para evitar duplicación.
 */
