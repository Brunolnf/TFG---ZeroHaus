firebase-functions>=0.4.0
firebase-admin>=6.5.0
# tzdata: necesario en Windows para que scheduler_fn.Timezone resuelva
# zonas IANA (Linux/Cloud Functions ya las trae). Sin esto, el deploy
# falla localmente al hacer el análisis del codebase.
tzdata>=2024.1
